# Cardiovascular-disease-predictor
Machine learning pipeline to predict cardiovascular disease using a dataset of 70,000 patient records. Compares five supervised ML classifiers (Random Forest, SVM, KNN, Decision Tree, and Logistic Regression). Optimizes features, handles outliers, and evaluates models using Accuracy, F1 score, and Recall (prioritizing recall for safety).
